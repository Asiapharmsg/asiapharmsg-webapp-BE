name: Deploy backend

# Every push to main (or a manual run) rebuilds api.asiapharmsg.com on the
# EC2 host. Credentials come from GitHub's OIDC token exchanged for the
# asiapharm-github-deploy role (see deploy/github-oidc-role.yml); nothing is
# stored in repository secrets.
on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

concurrency:
  group: deploy-backend
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::495278513840:role/asiapharm-github-deploy
          aws-region: ap-southeast-1

      - name: Package release archive
        run: git archive --format=tar.gz -o /tmp/be.tar.gz HEAD

      - name: Upload to S3
        run: aws s3 cp /tmp/be.tar.gz s3://asiapharmsg-db-backups/config/be.tar.gz

      - name: Deploy on EC2 via SSM
        run: bash deploy/run-remote.sh backend
